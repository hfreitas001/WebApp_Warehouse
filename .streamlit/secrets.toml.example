# O WMS usa login simples por e-mail (tela antes do app); não é necessário configurar nada aqui para isso.
# As movimentações gravam user_email na tabela operations_webapp_warehouse_movements.
#
# --- Opcional: se no futuro quiser usar Google OIDC, copie para secrets.toml e preencha. ---
# No Google Cloud Console:
# 1. APIs & Services > Credentials > Create Credentials > OAuth 2.0 Client ID
# 2. Application type: Web application
# 3. Authorized redirect URIs: https://SEU_APP.streamlit.app/oauth2callback (ou http://localhost:8501/oauth2callback para local)
# 4. Copie client_id e client_secret abaixo.
#
# Gere um cookie_secret forte (ex.: openssl rand -hex 32)

[auth]
redirect_uri = "http://localhost:8501/oauth2callback"
cookie_secret = "GERE_UM_SEGREDO_FORTE_32_BYTES_HEX"
client_id = "SEU_CLIENT_ID.apps.googleusercontent.com"
client_secret = "SEU_CLIENT_SECRET"
server_metadata_url = "https://accounts.google.com/.well-known/openid-configuration"
